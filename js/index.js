$(document).ready(function(){

	$('.b-1-slider').slick();

	$('.clients-slider').slick({
		autoplay: true,
		dots: true,
		arrows: false,
		slidesToShow: 3,
	});

	$('.b-6-slider').slick({
		autoplay: true,
		dots: true,
		arrows: false,
		slidesToShow: 3,
	});

});